### Notes

Built a Fahrenheit to Celsius converter, traced it with F = 212 (boiling water) to confirm the math output 100 C. Then made three modifications: flipped the direction (C to F), let the user pick which direction via prompt, and rounded to 1 decimal place with `.toFixed(1)`.

### Code

```javascript
// L1: C to F
const C = parseFloat(prompt("Celsius:"));
const F = (C * 9/5) + 32;
document.write(C + "°C = " + F + "°F");

// L2: user picks direction
const dir = prompt("Convert (1) F→C or (2) C→F?");
if (dir === "1") {
    // F to C math
} else {
    // C to F math
}

// L3: clean output
result.toFixed(1)  // "100.0" not "100.000..."
```

### What I learned

`.toFixed(n)` is the move for clean number output. Floats can return ugly long decimals by default.
